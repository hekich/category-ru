{
	"name": "category-ru",
	"module": "./src/index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"lint": "bunx biome check .",
		"lint:fix": "bunx biome check --write .",
		"rule-set:check": "bunx biome check rules/category-ru.json",
		"rule-set:format": "bunx biome check --write rules/category-ru.json",
		"typecheck": "bunx tsc --noEmit",
		"test": "bun test",
		"check": "bun run lint && bun run typecheck && bun run test"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.12",
		"@types/bun": "^1.3.12"
	},
	"peerDependencies": {
		"typescript": "^5.9.3"
	}
}
